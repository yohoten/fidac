# pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/

pandas>=2.0.0
numpy>=1.24.0
akshare>=1.12.0
openpyxl>=3.1.0
matplotlib>=3.7.0
scikit-learn>=1.3.0
prophet>=1.1.0

# 注: Prophet 不包含，因 CmdStan 在中文路径下无法运行
#     时序预测改用 sklearn.linear_model.LinearRegression 替代
