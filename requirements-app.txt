# ============================================================
# Streamlit 可视化看板 —— 精简依赖（云端部署用）
# ============================================================
# 说明：
#   - 本文件仅包含 streamlit_app.py 运行所需的最小依赖，用于
#     Streamlit Community Cloud 快速构建（不安装 akshare/matplotlib/prophet 等重依赖）。
#   - 数据已预采集为 data/*.xlsx，运行时无需联网采集。
#   - 在 Streamlit Community Cloud 部署时，进入
#       Advanced settings → Requirements file 填写 requirements-app.txt。
#   - 本地开发建议直接使用 requirements.txt（完整依赖）。
# ============================================================
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0
scikit-learn>=1.3.0
streamlit>=1.49
plotly>=5.18
